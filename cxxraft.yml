---
:scr: default.sh
:sources:
  avl.hxx:
    :deps:
      standart: mixin
      math: mixin
    :sample: default
  redblack.cxx:
    :deps:
      standart: mixin
      math: mixin
    :sample: default
  auto-completion.cxx:
    :deps:
      standart: mixin
      math: mixin
    :sample: default
  test_avl.cxx:
    :deps:
      catch: local
      standart: mixin
    :sample: test
